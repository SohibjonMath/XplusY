// Register Android FCM token for an authenticated OrzuMall customer.
const admin=require('firebase-admin');
const {tokenHash}=require('./_customerPush');
function initAdmin(){if(admin.apps.length)return;const b64=process.env.FIREBASE_SERVICE_ACCOUNT_B64;if(!b64)throw new Error('Missing env FIREBASE_SERVICE_ACCOUNT_B64');const serviceAccount=JSON.parse(Buffer.from(b64,'base64').toString('utf8'));admin.initializeApp({credential:admin.credential.cert(serviceAccount)});}
function json(statusCode,body){return{statusCode,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'},body:JSON.stringify(body)}}
function bearer(event){const h=event.headers?.authorization||event.headers?.Authorization||'';const m=h.match(/^Bearer\s+(.+)$/i);return m?m[1]:''}
exports.handler=async event=>{try{initAdmin();if(event.httpMethod!=='POST')return json(405,{ok:false,error:'method_not_allowed'});const idToken=bearer(event);if(!idToken)return json(401,{ok:false,error:'unauthorized'});let decoded;try{decoded=await admin.auth().verifyIdToken(idToken)}catch(_){return json(401,{ok:false,error:'bad_token'})}let body={};try{body=event.body?JSON.parse(event.body):{}}catch(_){return json(400,{ok:false,error:'invalid_json'})}const fcmToken=String(body.token||'').trim();if(fcmToken.length<40||fcmToken.length>4096)return json(400,{ok:false,error:'invalid_fcm_token'});const id=tokenHash(fcmToken),db=admin.firestore();await db.doc(`customerPushTokens/${id}`).set({token:fcmToken,uid:String(decoded.uid||''),email:String(decoded.email||'').slice(0,180),platform:'android',appId:String(body.appId||'uz.orzumall.app').slice(0,120),deviceName:String(body.deviceName||'').slice(0,180),active:true,createdAt:admin.firestore.FieldValue.serverTimestamp(),updatedAt:admin.firestore.FieldValue.serverTimestamp(),lastSeenAt:admin.firestore.FieldValue.serverTimestamp()},{merge:true});return json(200,{ok:true,id})}catch(e){return json(500,{ok:false,error:'server_error',detail:String(e?.message||e).slice(0,240)})}};
