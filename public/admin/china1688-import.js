import { auth } from '/firebase-config.js';
import { GoogleAuthProvider, onAuthStateChanged, signInWithPopup, signOut } from 'https://www.gstatic.com/firebasejs/10.12.4/firebase-auth.js';

const $=id=>document.getElementById(id);
const provider=new GoogleAuthProvider();
const state={source:null,products:[],editingId:'',pricing:{cnyToUzs:1850,servicePercent:12,reservePercent:3,minServiceUzs:15000},externalImages:[]};
const esc=s=>String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=n=>Math.round(Number(n||0)).toLocaleString('ru-RU')+' so‘m';
const cny=n=>Number(n||0).toLocaleString('ru-RU',{maximumFractionDigits:2})+' ¥';
let toastTimer;
function toast(text){$('toast').textContent=String(text||'');$('toast').classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('toast').classList.remove('show'),3200)}
function notice(text,type='info'){$('notice').hidden=!text;$('notice').className='notice '+type;$('notice').textContent=text||''}
function copyNotice(text,type='info'){$('copyStatus').hidden=!text;$('copyStatus').className='copy-status '+type;$('copyStatus').textContent=text||''}
function safeLines(v){return String(v||'').split(/\n+/).map(x=>x.trim()).filter(Boolean)}
function isStorageUrl(v){return /^https:\/\/firebasestorage\.googleapis\.com\//i.test(String(v||''))}
function normalizeImageUrl(v){
  let s=String(v||'').trim().replace(/&amp;/g,'&').replace(/\\u002F/gi,'/').replace(/\\\//g,'/');
  if(!s||s.startsWith('data:')||s.startsWith('blob:'))return'';
  try{const u=new URL(s.startsWith('//')?'https:'+s:s,location.href);if(u.protocol==='http:'&&/(?:^|\.)(?:alicdn\.com|1688\.com|tbcdn\.cn|alibabausercontent\.com)$/i.test(u.hostname))u.protocol='https:';u.hash='';return u.toString()}catch{return''}
}
function imageProxyUrl(v){const u=normalizeImageUrl(v);if(!u||isStorageUrl(u))return u;return '/.netlify/functions/china1688-image-proxy?url='+encodeURIComponent(u)}
function previewImg(u,extra=''){const original=normalizeImageUrl(u);if(!original)return'';return `<img src="${esc(imageProxyUrl(original))}" data-original-src="${esc(original)}" referrerpolicy="no-referrer" loading="lazy" decoding="async" ${extra}>`}
function armPreviewFallbacks(root=document){root.querySelectorAll('img[data-original-src]').forEach(img=>{if(img.dataset.fallbackReady)return;img.dataset.fallbackReady='1';img.addEventListener('error',()=>{if(img.dataset.directTried!=='1'){img.dataset.directTried='1';img.src=img.dataset.originalSrc;img.referrerPolicy='no-referrer'}else{img.classList.add('broken-preview');img.alt='Rasmni preview qilish imkoni bo‘lmadi'}})})}
function imageUrls(){const checked=[...document.querySelectorAll('#imagesGrid input:checked')].map(x=>normalizeImageUrl(x.value)).filter(Boolean);return [...new Set([...checked,...safeLines($('imagesText').value).map(normalizeImageUrl).filter(Boolean)])].slice(0,18)}
function calcAutoPrice(v){const p=state.pricing||{},base=Number(v||0)*Number(p.cnyToUzs||1850),service=Math.max(Number(p.minServiceUzs||15000),base*Number(p.servicePercent||0)/100),reserve=base*Number(p.reservePercent||0)/100;return Math.ceil((base+service+reserve)/500)*500}
function setBusy(btn,on,label){if(!btn)return;if(on){btn.dataset.old=btn.innerHTML;btn.disabled=true;btn.innerHTML='<i class="fa-solid fa-spinner fa-spin"></i> '+label}else{btn.disabled=false;btn.innerHTML=btn.dataset.old||label}}
async function api(body){const token=await auth.currentUser?.getIdToken();if(!token)throw new Error('Admin akkaunti bilan kiring');const r=await fetch('/.netlify/functions/china1688-import-admin',{method:'POST',headers:{'content-type':'application/json','authorization':'Bearer '+token},body:JSON.stringify(body)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||d.error||'Server xatosi');return d}
function extract1688Url(v){const s=String(v||'').trim(),urls=[s,...(s.match(/https?:\/\/[^\s<>"']+/gi)||[])];for(const raw of urls){try{const u=new URL(raw.replace(/[),.;]+$/,''));if(u.hostname==='1688.com'||u.hostname.endsWith('.1688.com'))return u.toString()}catch{}}return''}
function parseItemId(url){return String(url||'').match(/(?:offer\/|offerId=|itemId=|id=)(\d{6,})/i)?.[1]||''}
function defaultDescription(src){const rows=['Xitoydan buyurtma qilinadigan mahsulot. Yetkazib berish muddati taxminiy.'];(src?.props||[]).slice(0,12).forEach(p=>{if(p.name&&p.value)rows.push(`${p.name}: ${p.value}`)});return rows.join('\n')}
function renderImages(images=[]){const rows=[...new Set(images.map(normalizeImageUrl).filter(Boolean))];$('imagesGrid').innerHTML=rows.length?rows.map(u=>`<label class="image-choice${isStorageUrl(u)?' local':''}">${previewImg(u,'alt=""')}<input type="checkbox" value="${esc(u)}" checked>${isStorageUrl(u)?'<small>Storage</small>':''}</label>`).join(''):'<div class="empty-mini">Rasmlar topilmadi. Rasm URL’larini qo‘lda kiriting.</div>';armPreviewFallbacks($('imagesGrid'))}
function renderVariants(rows=[]){$('variantList').innerHTML=rows.length?rows.slice(0,36).map(v=>`<span>${esc(v.name||v.id||'Variant')}${v.priceCny?' · '+esc(cny(v.priceCny)):''}${v.stock?' · '+esc(v.stock)+' ta':''}</span>`).join(''):'<span>Variant topilmadi</span>'}
function markExistingBySource(){if(!state.source?.id||state.editingId)return;const old=state.products.find(x=>String(x.itemId||'')===String(state.source.id));if(old){state.editingId=old.id;$('editState').textContent='Yangilash: '+old.id;notice('Bu 1688 mahsuloti oldin import qilingan. Saqlasangiz mavjud karta yangilanadi.','info')}}
function applySource(src={},opts={}){state.source=src;state.externalImages=[...new Set((src.images||[]).map(normalizeImageUrl).filter(Boolean))];const images=state.externalImages;$('editor').hidden=false;$('open1688').href=src.url||$('sourceUrl').value||'#';$('sourceUrl').value=src.url||$('sourceUrl').value||'';$('sourceTitle').textContent=src.title||'Qo‘lda kiritiladigan 1688 mahsuloti';$('sourceMeta').textContent=[src.id?'1688 ID: '+src.id:'',src.sellerName||'',src.sellerLocation||'',src.extractorVersion?'Importer v'+src.extractorVersion:''].filter(Boolean).join(' · ')||'Manba ma’lumoti qo‘lda to‘ldiriladi';$('sourceCover').innerHTML=src.image?previewImg(src.image,'alt=""'):'<i class="fa-solid fa-image"></i>';armPreviewFallbacks($('sourceCover'));$('sourceChips').innerHTML=(src.serviceTags||[]).map(x=>`<span>${esc(x)}</span>`).join('');$('cnyLabel').textContent=src.priceCny?cny(src.priceCny):'—';$('autoPriceLabel').textContent=src.priceUzs?money(src.priceUzs):money(calcAutoPrice(src.priceCny));$('moqLabel').textContent=String(src.moq||1)+' ta';$('stockLabel').textContent=src.stock?Number(src.stock).toLocaleString('ru-RU')+' ta':'—';renderImages(images);renderVariants(src.variants||[]);if(!opts.keepForm){$('name').value=src.title||'';$('nameRu').value=src.title||'';$('priceCny').value=src.priceCny||'';$('price').value=src.priceUzs||calcAutoPrice(src.priceCny)||'';$('moq').value=src.moq||1;$('stock').value=src.stock||0;$('description').value=defaultDescription(src);$('imagesText').value=''}markExistingBySource();window.scrollTo({top:$('editor').offsetTop-86,behavior:'smooth'})}
function resetEditor(){state.source=null;state.externalImages=[];state.editingId='';$('editState').textContent='Yangi mahsulot';$('editor').hidden=true;copyNotice('');['name','nameRu','price','oldPrice','priceCny','weightKg','description','imagesText'].forEach(id=>$(id).value='');$('minDays').value=15;$('maxDays').value=30;$('moq').value=1;$('stock').value=0;$('popularScore').value=50;$('tags').value='1688, Xitoydan buyurtma, trend'}
function manual(){const url=extract1688Url($('sourceUrl').value);if(!url)return notice('Qo‘lda import qilish uchun haqiqiy 1688 havolasini kiriting.','error');state.editingId='';$('editState').textContent='Qo‘lda yangi mahsulot';applySource({id:parseItemId(url),url,title:'',images:[],priceCny:0,moq:1,stock:0,props:[],variants:[]});notice('Qo‘lda kiritish rejimi ochildi. Mahsulot ma’lumotlarini to‘ldiring.','info')}
function productPayload(finalImages=imageUrls()){const src={...(state.source||{}),url:extract1688Url($('sourceUrl').value),id:(state.source?.id||parseItemId($('sourceUrl').value)),priceCny:Number($('priceCny').value||0),moq:Number($('moq').value||1),stock:Number($('stock').value||0),images:state.externalImages.length?state.externalImages:finalImages};return{productId:state.editingId,sourceUrl:src.url,itemId:src.id,source:src,name:$('name').value.trim(),name_ru:$('nameRu').value.trim(),price:Number($('price').value||0),oldPrice:Number($('oldPrice').value||0),priceCny:Number($('priceCny').value||0),weightKg:Number($('weightKg').value||0),deliveryMinDays:Number($('minDays').value||15),deliveryMaxDays:Number($('maxDays').value||30),moq:Number($('moq').value||1),stock:Number($('stock').value||0),popularScore:Number($('popularScore').value||50),tags:$('tags').value.split(',').map(x=>x.trim()).filter(Boolean),images:finalImages,externalImages:state.externalImages,description:$('description').value.trim()}}
async function copyImagesToStorage(urls,itemId){const existing=urls.filter(isStorageUrl),remote=urls.filter(x=>!isStorageUrl(x));if(!remote.length)return{urls,failed:0,copied:0};const copied=[],failed=[];for(let i=0;i<remote.length;i+=6){const part=remote.slice(i,i+6);copyNotice(`Rasmlar Storage’ga ko‘chirilmoqda: ${Math.min(i+part.length,remote.length)} / ${remote.length}`,'info');const d=await api({action:'copyImages',itemId:itemId||'draft',urls:part});copied.push(...(d.copied||[]).map(x=>x.url));failed.push(...(d.failed||[]).map(x=>x.sourceUrl))}return{urls:[...existing,...copied,...failed].slice(0,18),copied:copied.length,failed:failed.length}}
async function save(){const btn=$('saveBtn');if(!$('name').value.trim()||!Number($('price').value))return toast('Mahsulot nomi va sotuv narxini kiriting');const original=imageUrls();if(!original.length&&!confirm('Rasmsiz mahsulot saqlansinmi?'))return;setBusy(btn,true,'Saqlanmoqda...');try{let finalImages=original;let copyResult={copied:0,failed:0};if($('copyImages').checked&&original.length){copyResult=await copyImagesToStorage(original,state.source?.id||parseItemId($('sourceUrl').value));finalImages=copyResult.urls;copyNotice(copyResult.failed?`${copyResult.copied} ta rasm Storage’ga nusxalandi. ${copyResult.failed} ta rasm tashqi URL sifatida qoldi.`:`${copyResult.copied} ta rasm Firebase Storage’ga muvaffaqiyatli nusxalandi.`,copyResult.failed?'warning':'success')}const d=await api({action:'save',product:productPayload(finalImages)});toast(d.created?'Mahsulot katalogga qo‘shildi':'Mahsulot yangilandi');notice(`Mahsulot ${d.id} ID bilan saqlandi. Katalog pullik API’siz ishlaydi.`,'success');resetEditor();await loadList()}catch(e){toast(e.message);copyNotice(e.message,'error')}finally{setBusy(btn,false,'Katalogga saqlash')}}
function savedCard(p){return `<article class="saved-card"><div>${p.image?previewImg(p.image,'alt=""'):'<div class="placeholder"><i class="fa-solid fa-image"></i></div>'}</div><div><h3>${esc(p.name||'Mahsulot')}</h3><p><b class="amount">${esc(money(p.price))}</b> · ID: ${esc(p.id)}</p><p>${esc(p.itemId?'1688: '+p.itemId:'1688 havola')} · ${p.isActive?'Faol':'Arxiv'}</p><p><i class="fa-solid fa-cloud"></i> Storage: ${esc(p.localImageCount||0)} ta rasm</p><div class="saved-actions"><button class="tiny-btn" data-edit="${esc(p.id)}"><i class="fa-solid fa-pen"></i> Tahrir</button>${p.sourceUrl?`<a class="tiny-btn" href="${esc(p.sourceUrl)}" target="_blank" rel="noopener"><i class="fa-solid fa-arrow-up-right-from-square"></i> 1688’da ochish</a>`:''}${p.isActive?`<button class="tiny-btn danger" data-archive="${esc(p.id)}"><i class="fa-solid fa-box-archive"></i> Arxiv</button>`:''}</div></div></article>`}
function renderList(){const rows=state.products;$('savedCount').textContent=rows.length+' ta';$('savedGrid').innerHTML=rows.map(savedCard).join('');$('empty').hidden=!!rows.length;armPreviewFallbacks($('savedGrid'));markExistingBySource()}
async function loadList(){if(!auth.currentUser)return;try{const d=await api({action:'list'});state.products=d.products||[];state.pricing=d.pricing||state.pricing;renderList()}catch(e){toast(e.message)}}
function editRow(id){const p=state.products.find(x=>x.id===id);if(!p)return;state.editingId=id;$('editState').textContent='Tahrirlash: '+id;$('sourceUrl').value=p.sourceUrl||'';applySource(p.source||{url:p.sourceUrl,id:p.itemId,images:p.images},{keepForm:true});state.externalImages=p.source?.images||p.images||[];$('name').value=p.name||'';$('nameRu').value=p.name_ru||'';$('price').value=p.price||'';$('oldPrice').value=p.oldPrice||0;$('priceCny').value=p.priceCny||p.source?.priceCny||'';$('weightKg').value=p.weightKg||'';$('minDays').value=p.deliveryMinDays||15;$('maxDays').value=p.deliveryMaxDays||30;$('moq').value=p.moq||1;$('stock').value=p.source?.stock||0;$('popularScore').value=p.popularScore||50;$('tags').value=(p.tags||[]).join(', ');$('description').value=p.description||''}
async function archiveRow(id){if(!confirm('Mahsulot katalogdan yashirilsinmi?'))return;try{await api({action:'archive',productId:id});toast('Mahsulot arxivlandi');await loadList()}catch(e){toast(e.message)}}
function decodeBase64Url(v){const pad='='.repeat((4-v.length%4)%4),raw=atob(String(v).replace(/-/g,'+').replace(/_/g,'/')+pad),bytes=Uint8Array.from(raw,c=>c.charCodeAt(0));return new TextDecoder().decode(bytes)}
function acceptExtensionImport(){const match=location.hash.match(/(?:^#|&)om1688=([^&]+)/);if(!match)return;try{const packet=JSON.parse(decodeBase64Url(match[1]));const src=packet?.item;if(!src?.url||!extract1688Url(src.url))throw new Error('Importer ma’lumoti noto‘g‘ri');history.replaceState(null,'',location.pathname+location.search);state.editingId='';$('editState').textContent='Kengaytmadan yangi qoralama';$('extensionReady').classList.add('received');$('extensionReady').innerHTML='<i class="fa-solid fa-circle-check"></i><div><b>Kengaytmadan mahsulot olindi</b><p>Ma’lumotlarni tekshirib, sotuv narxini belgilang va katalogga saqlang.</p></div>';$('sourceUrl').value=src.url;applySource(src);notice('1688 sahifasidagi ma’lumot API ishlatmasdan qoralama sifatida olindi. Tekshirib saqlang.','success')}catch(e){notice('Kengaytmadan kelgan qoralama ochilmadi: '+e.message,'error')}}
$('manualBtn').addEventListener('click',manual);$('saveBtn').addEventListener('click',save);$('refreshBtn').addEventListener('click',loadList);$('loginBtn').addEventListener('click',()=>signInWithPopup(auth,provider).catch(e=>toast(e.message)));$('authBtn').addEventListener('click',()=>auth.currentUser?signOut(auth):signInWithPopup(auth,provider).catch(e=>toast(e.message)));$('priceCny').addEventListener('input',()=>{$('autoPriceLabel').textContent=money(calcAutoPrice($('priceCny').value))});$('savedGrid').addEventListener('click',e=>{const edit=e.target.closest('[data-edit]'),archive=e.target.closest('[data-archive]');if(edit)editRow(edit.dataset.edit);if(archive)archiveRow(archive.dataset.archive)});
onAuthStateChanged(auth,u=>{$('loginBox').hidden=!!u;$('authBtn').innerHTML=u?'<i class="fa-solid fa-right-from-bracket"></i><span>Chiqish</span>':'<i class="fa-brands fa-google"></i><span>Kirish</span>';if(u)loadList();else{state.products=[];renderList()}});
renderList();acceptExtensionImport();
